**Open in Xcode** opens the current Nova workspace in Xcode.

## Usage

Run **Extensions > Open in Xcode** from the menu bar, or use the Command Palette.

## Requirements

Xcode must be installed on your Mac.
